import {StyleSheet} from 'react-native';
import {windowHeight} from '@theme/appConstant';

export default dividerStyles = StyleSheet.create({
  divide: {
    width: '100%',
  },
  darkDivider: {
    width: '100%',
    height: windowHeight(12),
  },
});
